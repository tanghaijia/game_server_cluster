# For details on buf.yaml configuration, visit https://buf.build/docs/configuration/v2/buf-yaml
version: v2
# 定义 workspace modules，让 proto 文件之间的 import 能跨模块解析
modules:
  - path: .
  - path: ../asset_service/proto
  - path: ../node_agent/proto
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
